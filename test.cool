let 
    a: Int <- 987,
    b: String <- "NoClass\tTest\n\\Escape",
    c: Bool <- not (123 < 456),
    d: MyType <- new MyType
in {
    if c then {
        d.show(a - b.size());
    } else {
        loop a <- a + 1 pool;
    } fi;

    case a of
        5 => d.msg("Five");
        10 => d.msg("Ten");
        _ => d.msg("Other");
    esac;
};

! ^ & *
